services:
  - type: web
    name: naina-vai-backend
    env: python
    runtime: python-3.11.0
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend_server:app
    envVars:
      - key: GROQ_API_KEY
        sync: false
    plan: free
